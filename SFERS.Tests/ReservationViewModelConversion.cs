namespace SFERS.Utilities
{
    public class ReservationViewModelConversion
    {
        private ReservationManager reservationManager;
        [SetUp]
        public void Setup()
        {
            reservationManager = new ReservationManager(null);
        }
        #region Conversion Tests

        #endregion
    }
}